import 'package:flutter/material.dart';

class TourStop3 extends StatelessWidget {
  const TourStop3({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Tour stop 3'),
    );
  }
}
